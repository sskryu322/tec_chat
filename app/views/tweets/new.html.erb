<%= form_with model: @tweet, local: true do |form| %>
  <%= form.text_field :title, placeholder: "タイトル" %><br>
  <%= form.text_area :content, placeholder: "投稿内容"  %><br>
  <%= form.text_field :name, placeholder: "質問者名"  %><br>
  <%= form.submit 'POST' %>
<% end %>